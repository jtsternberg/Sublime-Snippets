<snippet>
	<content><![CDATA[
window.${1:YourObject} = window.${1:YourObject} || {};

( function( window, document, \$, app, undefined ) {
	'use strict';

	app.cache = function() {
		app.\$ = {};
	};

	app.init = function() {
		app.cache();

		${4:// put all your jQuery goodness in here.}
	};

	\$( app.init );

} )( window, document, jQuery, window.${1:YourObject} );
]]></content>
	<tabTrigger>jqueryanon</tabTrigger>
</snippet>
